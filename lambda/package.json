{
  "name": "roastmylanding-lambda",
  "version": "1.0.0",
  "description": "Lambda function for RoastMyLanding roast generation",
  "main": "roast-handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1500.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}